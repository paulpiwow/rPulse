/**
 * JPA entities for the hierarchy domain (OWNER: Ty) — mapped to the tables in
 * {@code V2__hierarchy.sql}: site, asset, machine, data_source, tag, ctag,
 * baseline_rule. Each entity extends {@link com.rpulse.backend.common.BaseEntity}
 * and declares only its domain columns.
 */
package com.rpulse.backend.hierarchy.entity;
