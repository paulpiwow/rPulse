{
  "name": "rpulse-cadre-demo",
  "version": "2.0.0-demo",
  "private": true,
  "type": "module",
  "description": "Static rhoPulse CADRE demo prototype.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "node scripts/check.mjs",
    "seed": "node scripts/seed.mjs"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
